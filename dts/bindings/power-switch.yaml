description: GPIO pin to switch a power output on or off

compatible: "power-switch"

properties:
  gpios:
    type: phandle-array
    required: true
    description: |
      The GPIO connected to the load driver.

  label:
    type: string
    required: false
    description: |
      A textual description of the device connected to the GPIO